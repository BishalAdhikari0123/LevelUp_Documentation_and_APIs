meta {
  name: chat
  type: http
  seq: 1
}

post {
  url: {{base_url}}/ai/chat
  body: json
  auth: inherit
}

body:json {
  {
  "prompt":"hi, what is your name"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
