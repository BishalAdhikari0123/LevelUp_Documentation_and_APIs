meta {
  name: Join Clan
  type: http
  seq: 2
}

post {
  url: {{base_url}}/clan/join
  body: json
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

body:json {
  {
    "clanId": "your-clan-id-here"
  }
}
